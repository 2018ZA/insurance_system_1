<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/tx
       https://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- Включить сканирование компонентов и аннотаций -->
    <context:component-scan base-package="com.insurance_system"/>
    <context:annotation-config/>
    
    <!-- Загрузка свойств из внешнего файла (БЕЗОПАСНОСТЬ И ГИБКОСТЬ) -->
    <context:property-placeholder location="classpath:database.properties"/>

    <!-- НАСТРОЙКА ПУЛА СОЕДИНЕНИЙ (ПРОИЗВОДИТЕЛЬНОСТЬ) -->
    <bean id="dataSource" class="org.apache.commons.dbcp2.BasicDataSource" destroy-method="close">
        <property name="driverClassName" value="org.postgresql.Driver"/>
        <property name="url" value="${database.url}"/>
        <property name="username" value="${database.username}"/>
        <property name="password" value="${database.password}"/>
        <!-- Настройки пула -->
        <property name="initialSize" value="5"/>
        <property name="maxTotal" value="20"/>
        <property name="maxIdle" value="10"/>
        <property name="minIdle" value="5"/>
        <property name="maxWaitMillis" value="30000"/>
    </bean>

    <!-- ФАБРИКА СЕССИЙ HIBERNATE -->
    <bean id="sessionFactory" class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="packagesToScan" value="com.insurance_system.entity"/>
        <property name="hibernateProperties">
            <props>
                <!-- Диалект для PostgreSQL -->
                <prop key="hibernate.dialect">org.hibernate.dialect.PostgreSQL10Dialect</prop>
                <!-- Отображение SQL в логах (удобно для отладки) -->
                <prop key="hibernate.show_sql">true</prop>
                <prop key="hibernate.format_sql">true</prop>
                <!-- Стратегия обновления схемы: update / validate / create-drop -->
                <prop key="hibernate.hbm2ddl.auto">update</prop>
                <!-- Кодировка -->
                <prop key="hibernate.connection.charSet">UTF-8</prop>
                <!-- Оптимизация для работы с коллекциями -->
                <prop key="hibernate.enable_lazy_load_no_trans">false</prop>
                <!-- Второй уровень кэша (опционально) -->
                <prop key="hibernate.cache.use_second_level_cache">false</prop>
                <prop key="hibernate.cache.use_query_cache">false</prop>
            </props>
        </property>
    </bean>

    <!-- ДИСПЕТЧЕР ТРАНЗАКЦИЙ (КРИТИЧЕСКИ ВАЖНО!) -->
    <bean id="transactionManager" class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <!-- ВКЛЮЧЕНИЕ УПРАВЛЕНИЯ ТРАНЗАКЦИЯМИ ЧЕРЕЗ АННОТАЦИИ -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

</beans>